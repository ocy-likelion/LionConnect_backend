# LionConnect 커넥트 요청 기능 추가 의존성
# 기존 requirements.txt에 추가하거나 별도로 설치

# 이메일 검증을 위한 추가 의존성
email-validator==2.2.0

# 기존 의존성들 (이미 설치되어 있을 수 있음)
fastapi==0.115.14
uvicorn==0.35.0
sqlalchemy==2.0.41
pydantic==2.11.7
requests==2.32.4
python-multipart==0.0.20
jinja2==3.1.6
aiofiles==24.1.0 